services:
  proxy:
    build: .
    image: free-claude-code-ai
    container_name: free-claude-code-ai
    ports:
      - "${PORT:-3000}:3000"
    env_file: .env
    volumes:
      - ./.data:/app/.data
      - ./logs:/app/logs
    restart: unless-stopped
